---
// You can replace this with your actual video path
const videoSource = '/media/hero-bg.mp4';
---

<section class="relative h-[100dvh] w-full overflow-hidden">
  <!-- Video Background -->
  <div class="absolute inset-0 w-full h-full">
    <video
      poster="/media/hero-poster.webp"
      preload="auto"
      autoplay
      muted
      loop
      playsinline
      class="w-full h-full object-cover object-[75%_50%] md:object-center"
    >
      <source src={videoSource} type="video/mp4" />
      Tu navegador no soporta la reproducción de video.
    </video>
    <!-- Overlay for better text readability -->
    <div class="absolute inset-0 bg-black/40"></div>
  </div>

  <!-- Content -->
  <div class="relative h-full flex items-end">
    <div class="container mx-auto px-4 pb-16 md:pb-24">
      <div class="max-w-3xl">
        <h1 class="text-4xl md:text-7xl font-bold text-white mb-4">
          PIYALI Integración Ambiental
        </h1>
        <p class="text-xl md:text-2xl text-white/90 max-w-2xl">
          Servicios de consultoría ambiental de alta calidad al mejor costo en
          la Ciudad de México y Estado de México.
        </p>
      </div>
    </div>
  </div>
</section>
